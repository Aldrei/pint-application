import * as React from 'react';

/* istanbul ignore next */ 
// eslint-disable-next-line @typescript-eslint/no-empty-function
const SnackContext = React.createContext({ addMessage: () => {} });

export default SnackContext;
